import React from 'react'
// import { connect } from 'react-redux'

const Input = (props) => (
  <div>
    <form>
      <input type="text" name="search" placeholder="new todo list"/><button>send</button>
    </form>
  </div>
)


export default Input
